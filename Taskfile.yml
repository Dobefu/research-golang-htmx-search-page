version: '3'

tasks:
  default:
    cmds:
      - task livereload
  livereload:
    cmds:
      - pnpm exec tailwindcss -i ./assets/src/css/main.css -o ./assets/dist/css/main.css
      - templ generate
      - go build -o ./tmp/main .
